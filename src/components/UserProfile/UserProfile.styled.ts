import styled from '@emotion/styled'

export const UserProfile = styled.div`
	display: flex;
	align-items: center;

	span {
		font-weight: 700;
		margin: 0 10px;
	}

	svg path {
		stroke: var(--color-gray-100);
	}
`
